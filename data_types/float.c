#include<stdio.h>
// need more experiment

int main()
{
   float a= 9.1f;//f is for declaring float. otherwise it will convert into double and will take 64 bits of memory instead of 32 bit
   float x= 3.1f;
    
    float y=3E-2f;

    printf("%f\n %.2f\n %f",a,x,y);

    return 0;
}